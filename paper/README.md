# Paper draft

Compile the manuscript from the repository root with:

```sh
typst compile paper/main.typ paper/main.pdf
```

The draft is derived from the current FEMTools.jl residual and solver
implementations. Validation tables, benchmark figures, affiliations, and the
final reference for the automatic-tuning method remain to be added before
submission.
